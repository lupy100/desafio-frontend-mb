{
  "name": "server",
  "version": "1.0.0",
  "description": "Backend para o formulário",
  "main": "src/index.js",
  "type": "module",
  "scripts": {
    "dev": "nodemon src/index.js",
    "build": "rm -rf dist && mkdir dist && cp -r src/* dist/",
    "start": "node src/index.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.21.2"
  },
  "devDependencies": {
    "nodemon": "^3.1.9"
  }
}
